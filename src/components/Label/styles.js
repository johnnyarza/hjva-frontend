import styled from 'styled-components';

export const Container = styled.label`
  .label-text {
    position: absolute;
    z-index: 1;
    top: -8px;

    font-weight: 600;

    border-radius: 5px;

    padding: 0 5px 0 5px;
    color: #95a5a6;
    background-color: #ebebeb;
  }
`;
